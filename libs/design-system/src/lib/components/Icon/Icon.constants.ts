import { IconSize } from './Icon.types';

// values that define in figma for each size(sm,md,lg)
export const SIZE_VALUES: Record<IconSize, number> = {
  xs: 12,
  sm: 16,
  md: 20,
  lg: 24,
  lg_plus: 32,
  xl: 48,
};

export const STROKE_VALUES: Record<IconSize, number> = {
  xs: 1,
  sm: 1.2,
  md: 1.5,
  lg: 1.8,
  lg_plus: 2,
  xl: 5.5,
};
